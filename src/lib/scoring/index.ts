/**
 * Public entry point for component scoring.
 *
 * The implementation lives in ./components so components, combos and builds
 * can share the same domain-level layout without changing existing imports.
 */
export * from './components';
